# CLAUDE.md

This repository's canonical instructions live in [`AGENTS.md`](AGENTS.md). Read it in
full and follow it before making any change here.

If you encounter a more specific `AGENTS.md` file lower in the directory tree while
working, follow that file too, for the scope it covers, in addition to the root
`AGENTS.md`.

This file exists only so Claude Code loads repository instructions automatically. It
intentionally does not duplicate them. If anything here ever conflicts with
`AGENTS.md`, `AGENTS.md` wins.
